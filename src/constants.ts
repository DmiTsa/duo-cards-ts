export enum pages {
    greeting = 'greeting',
    currentGame = 'currentGame',
    selfLevels = 'selfLevels',
    gameOver = 'gameOver'
}

 export enum gameType {
    easy = 'easy',
    normal = 'normal',
    hard = 'hard',
    custom = 'custom'
}

export enum imgLibs {
    hearts = 'Hearts',
    default = 'Calendars'
}
